---
import Layout from '../layouts/Layout.astro';
---

<Layout>
  <!-- Líneas dinámicas de fondo -->
  <div class="absolute inset-0 z-0">
    <svg
      class="w-full h-full opacity-20"
      viewBox="0 0 1440 600"
      preserveAspectRatio="none"
      xmlns="http://www.w3.org/2000/svg"
    >
      <!-- Líneas curvas -->
      <path
        d="M0 300Q240 400 480 300T960 300T1440 400"
        fill="none"
        stroke="#3B82F6"
        stroke-width="2"
        stroke-dasharray="6 6"
      />
      <path
        d="M0 400Q240 500 480 400T960 400T1440 500"
        fill="none"
        stroke="#3B82F6"
        stroke-width="2"
        stroke-dasharray="6 6"
      />
      <path
        d="M0 200Q240 100 480 200T960 200T1440 100"
        fill="none"
        stroke="#3B82F6"
        stroke-width="2"
        stroke-dasharray="6 6"
      />
    </svg>
  </div>

  <!-- Hero Section para la página de contacto -->
  <section class="relative py-32 overflow-hidden bg-transparent">
    <div class="container mx-auto px-6 text-center relative z-10">
      <h1 class="text-5xl md:text-6xl font-bold text-white mb-10 ">
        Contáctanos
      </h1>
      <p class="text-xl text-gray-400 max-w-2xl mx-auto mb-8">
        Estamos aquí para ayudarte. Ponte en contacto con nosotros para obtener más información sobre nuestros productos y servicios.
      </p>
    
    </div>
  </section>

  <!-- Sección de Información de Contacto -->
  <section id="contact-info" class="relative py-20 overflow-hidden bg-transparent">
    <div class="container mx-auto px-6">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
        <!-- Información de contacto -->
        <div class="text-left">
          <h2 class="text-4xl font-bold text-white mb-8">
            ¿Cómo podemos ayudarte?
          </h2>
          <p class="text-gray-400 mb-8">
            Estamos disponibles para responder tus preguntas y brindarte la mejor atención. No dudes en contactarnos a través de cualquiera de los siguientes medios:
          </p>

          <!-- Detalles de contacto -->
          <div class="space-y-6">
            <div class="flex items-center">
              <span class="text-blue-500 text-2xl mr-4">📍</span>
              <p class="text-gray-400">
                Carrera 79 a #35-74 Sur Bogotá DC, Colombia
              </p>
            </div>
            <div class="flex items-center">
              <span class="text-blue-500 text-2xl mr-4">📞</span>
              <a href="tel:+573168341338" class="text-gray-400 hover:text-blue-500 transition duration-300">
                +57 316 834 1338
              </a>
            </div>
            <div class="flex items-center">
              <span class="text-blue-500 text-2xl mr-4">✉️</span>
              <a href="mailto:info@mundialquirurgicos.com" class="text-gray-400 hover:text-blue-500 transition duration-300">
                info@mundialquirurgicos.com
              </a>
            </div>
          </div>

          <!-- Botones de contacto -->
          <div class="mt-8 grid grid-cols-1 sm:grid-cols-2 gap-4">
            <!-- Botón de WhatsApp con mensaje predeterminado -->
            <a
              href="https://wa.me/573168341338?text=Hola,%20estoy%20interesado%20en%20sus%20productos.%20¿Podrían%20brindarme%20más%20información?"
              target="_blank"
              class="flex items-center justify-center bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition duration-300 transform hover:scale-105"
            >
              <span class="mr-2">💬</span> WhatsApp
            </a>

            <!-- Botón de Correo (copiar correo) -->
            <button
              id="copyEmailButton"
              class="flex items-center justify-center bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition duration-300 transform hover:scale-105"
            >
              <span class="mr-2">✉️</span> Copiar Correo
            </button>
          </div>

          <!-- Mensaje de "Correo copiado" -->
          <div id="copyEmailMessage" class="hidden mt-4 text-green-500 font-semibold">
            Correo copiado ✅
          </div>
        </div>

        <!-- Mapa de Google -->
        <div class="rounded-xl overflow-hidden shadow-2xl hover:shadow-blue-500/20 transition-all duration-300">
          <iframe
            src="https://www.google.com/maps?q=Carrera+79+a+%2335-74+Sur+Bogot%C3%A1+DC&output=embed"
            width="100%"
            height="400"
            style="border:0;"
            allowfullscreen=""
            loading="lazy"
          ></iframe>
        </div>
      </div>
    </div>
  </section>

  <!-- Script para copiar el correo -->
  <script>
    // Función para copiar el correo
    document.getElementById('copyEmailButton').addEventListener('click', function () {
      const email = 'info@mundialquirurgicos.com'; // Cambia este correo
      navigator.clipboard.writeText(email).then(() => {
        const message = document.getElementById('copyEmailMessage');
        message.classList.remove('hidden');
        setTimeout(() => {
          message.classList.add('hidden');
        }, 2000); // Oculta el mensaje después de 2 segundos
      });
    });
  </script>
</Layout>